@page
@model ErrorModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="h3 text-danger">Something went wrong</h1>
<p>Try again or check the application logs.</p>

@if (Model.ShowRequestId)
{
    <p class="small text-muted">Request ID: <code>@Model.RequestId</code></p>
}
